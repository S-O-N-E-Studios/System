# Use real backend (set to false when DB is set up). Omit or true = mock auth only.
# VITE_USE_MOCK_AUTH=false
VITE_API_BASE_URL=http://localhost:5000/api
VITE_GOOGLE_MAPS_API_KEY=your-google-maps-api-key
