# Frontend

React application for the Project Management app.

## Setup

1. Copy `.env.example` to `.env` and set `REACT_APP_API_URL`.
2. `npm install`
3. `npm start`

## Scripts

- `npm start` - Development server
- `npm run build` - Production build
- `npm test` - Run tests
- `npm run lint` - Lint code
