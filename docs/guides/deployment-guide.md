# Deployment Guide

(To be filled during deployment phase.)

- Backend: Node.js host (e.g. Heroku, Railway, Render)
- Frontend: Static host or same app (e.g. Vercel, Netlify)
- Database: MongoDB Atlas recommended for production
