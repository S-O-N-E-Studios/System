/**
 * routes/activity.routes.js
 *
 * NOTE: App wiring currently imports this module, but activity endpoints are
 * primarily mounted under other route modules (e.g. `project.routes.js`).
 * This file exists to satisfy the import so the app can boot and tests can run.
 */
const router = require('express').Router();

module.exports = router;

